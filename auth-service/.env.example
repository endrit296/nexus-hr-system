# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/nexus_auth

# Secret key used to sign JWTs — change this to a long random string in production
JWT_SECRET=nexus_jwt_secret_change_in_production

# Port the auth service listens on
PORT=3001
